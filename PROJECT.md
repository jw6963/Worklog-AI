# Worklog AI

매일 작성한 업무일지를 축적하고 완료한 일과 배운 점을 기간·프로젝트별로 다시 살펴보는 개인 및 소규모 팀용 업무 기록 서비스다. 장기적으로는 원문 기록에 근거한 AI 회고와 요약을 제공한다.

이 문서는 제품 방향과 현재 구현 상태를 관리한다. 실행 방법은 `README.md`, 실제 배포 절차는 `DEPLOYMENT.md`를 기준으로 한다.

## 제품 원칙

- 하루에는 하나의 일지가 있고, 그 안에 여러 업무 항목이 존재한다.
- 프로젝트는 일지 전체가 아니라 각 업무 항목에 지정한다.
- 완료한 일뿐 아니라 할 일, 메모와 그 맥락을 함께 기록한다.
- Markdown을 몰라도 노션처럼 자연스럽게 작성할 수 있어야 한다.
- 기간·프로젝트·항목 유형을 기준으로 이전 기록을 쉽게 찾아야 한다.
- 사용자의 기록은 언제든 표준 파일 형식으로 내보내고 복원할 수 있어야 한다.
- AI가 만든 문장에는 근거가 된 날짜와 원문 기록을 연결한다.
- 로컬 개발 데이터와 운영 데이터는 명확히 분리한다.

## 현재 구현 상태

### 인증과 사용자

- 비로그인 사용자용 소개 화면과 로그인 화면
- 관리자만 사용자 계정 생성 가능
- `ADMIN`, `USER` 역할 구분
- 임시 비밀번호로 최초 로그인한 사용자의 비밀번호 변경 강제
- 로그인 후 사용자 본인의 비밀번호 변경
- 관리자의 사용자 비밀번호 초기화 및 계정 활성화·비활성화
- 사용자별 일지, 프로젝트와 백업 데이터 분리

공개 회원가입은 지원하지 않는다.

### 일지 작성

- 날짜별 단일 일지
- `TODO`, `DONE`, `NOTE` 유형별 항목 작성
- 노션형 Markdown 편집과 미리보기
- `# `, `- ` 등 Markdown 입력 패턴의 즉시 표현
- Tab 및 Shift+Tab 들여쓰기
- 자동 저장과 편집 취소
- 항목 유형 변경, 삭제 및 실행 취소
- 전날 미완료 할 일 가져오기
- 이월된 할 일의 날짜 간 흐름 연결, 최신 TODO만 집계 및 실제 완료 날짜 표시
- 최신 이월 항목의 한 단계 이월 취소
- 날짜 이동 및 유형별 작성 위치 바로가기
- 긴 문서의 가로 확장 방지와 반응형 레이아웃

### 홈과 모아보기

- 최근 일지와 업무 현황 요약
- 유형별 항목 개수
- 1주, 2주, 한 달 또는 직접 지정한 기간 조회
- 키워드, 프로젝트와 항목 유형 필터
- 날짜가 많은 경우 페이지 단위 조회
- 검색 결과에서 원문 항목 위치로 이동 및 포커스
- 모든 주요 화면의 최상단 이동 버튼

### 프로젝트

- 프로젝트 생성
- 이름과 라벨 색상 변경
- 프로젝트 보관 및 복원
- 프로젝트에 연결된 항목 수 확인
- 프로젝트 삭제 시 기존 항목은 유지하고 프로젝트 연결만 해제
- 항목별 프로젝트 라벨 표시 및 변경

### 회고와 데이터 관리

- 주간 및 월간 통계와 규칙 기반 요약
- 이전 기간 대비 완료·미완료·메모·완료율 변화
- 프로젝트별 기록 분포와 오래 남은 TODO 탐지
- 업무량·완료 흐름·기록 지속률·프로젝트 집중도·회고 습관·지연 업무·분류 상태를 조합한 규칙 기반 기간 평가
- Markdown 일지 내보내기 및 가져오기
- 파일명 형식이 다른 Markdown의 날짜 확인 및 지정
- 전체 JSON 백업과 복원
- JSON 스키마 v1~v3 복원과 프로젝트·이월 흐름 보존
- 사용자별 백업 범위 분리
- 로그인 사용자를 위한 화면 내 사용 가이드

## 데이터 구조

### AppUser

- 사용자 ID, 로그인 ID, 표시 이름
- BCrypt 비밀번호
- 역할과 활성화 상태
- 최초 비밀번호 변경 필요 여부

### WorkItem

- ID와 업무 날짜
- 유형: `TODO`, `DONE`, `NOTE`
- Markdown 내용
- 소유 사용자
- 선택 프로젝트
- 생성·수정 시각
- 이월 흐름 ID, 이월 대상 날짜, 현재 날짜와 실제 완료 날짜

별도의 `DailyLog` 테이블을 만들지 않고, 사용자와 날짜가 같은 `WorkItem` 묶음을 하루 일지로 취급한다.

### Project

- ID, 이름, 색상
- 보관 여부
- 소유 사용자

## 기술 구성

### Frontend

- React 19, TypeScript, Vite
- React Router
- Fetch API와 세션 쿠키 인증
- 화면·기능별 `pages`, `components`, `api`, `styles` 구조
- 일반 CSS를 역할 및 화면 단위로 분리

### Backend

- Java 21, Spring Boot 4.1
- Spring Web MVC, Spring Security
- Spring Data JPA, Bean Validation
- 기능별 `auth`, `config`, `user`, `project`, `workitem` 패키지
- Maven Wrapper

### 저장소와 배포

- 로컬 개발: H2 파일 데이터베이스
- 운영 체험: Render PostgreSQL
- 웹 애플리케이션: Render Docker Web Service
- GitHub Actions: 프론트 lint·build와 백엔드 test
- Docker 다단계 빌드로 React 결과물을 Spring Boot JAR에 포함

## 프로젝트 구조

```text
Worklog-AI/
├── frontend/
│   └── src/
│       ├── api/
│       ├── auth/
│       ├── components/
│       │   ├── editor/
│       │   ├── import/
│       │   ├── layout/
│       │   └── ui/
│       ├── pages/
│       ├── styles/
│       ├── types/
│       └── utils/
├── backend/
│   └── src/main/java/com/worklog/backend/
│       ├── auth/
│       ├── config/
│       ├── project/
│       ├── user/
│       └── workitem/
├── DEPLOYMENT.md
├── Dockerfile
├── render.yaml
├── PROJECT.md
└── README.md
```

## 현재 단계

로컬 MVP와 사용자 인증, 데이터 분리, 운영 DB 및 배포 설정까지 구현되었다. 현재 작업은 `feature/deployment` 브랜치에 있으며 실제 배포 전 단계다.

남은 배포 작업:

1. `feature/deployment` 변경을 검토하고 `develop`, `main`에 병합
2. Render Blueprint에서 Web Service와 무료 PostgreSQL 생성
3. 최초 관리자 비밀번호 Secret 입력
4. 최초 관리자 로그인과 비밀번호 변경 확인
5. 기록 저장 및 재배포 후 데이터 유지 확인
6. JSON 백업 파일을 로컬과 허용된 클라우드에 보관
7. 무료 DB 만료 전 유료 전환 또는 외부 PostgreSQL 이전 여부 결정

## 다음 로드맵

### 1. 운영 안정화

- 실제 배포 환경에서 로그인·세션·DB 연결 검증
- Flyway를 통한 명시적 DB 스키마 마이그레이션
- 주요 조회 화면의 오류·로딩·재시도 경험 보완
- JSON 백업 주기 알림 또는 자동화
- 보안 헤더, 로그인 실패 제한과 운영 로그 점검

### 2. 사용성 개선

- 실제 일지 사용을 통한 에디터 세부 동작 개선
- 긴 일지의 유형별 접기·펼치기와 Alt+1·2·3 입력기 이동
- 자동 저장 상태와 실패 후 재저장 동작 명확화
- 대량 기록 환경의 검색과 페이지 이동 개선
- 태그 또는 프로젝트 외 분류 체계의 필요성 검증
- 모바일 작성 경험 보완

### 3. AI 기능

- 일간·주간·월간 요약
- 기간별 성과 추출
- 반복되는 장애물과 계속 미뤄지는 업무 탐지
- 배운 점과 주요 의사결정 추출
- 다음 기간 우선순위 제안
- 업무 보고서 및 성과평가 문장 생성
- AI 결과에서 근거 날짜와 원문 항목으로 이동

AI 기능은 충분한 실제 기록이 쌓이고 운영 안정성이 확인된 후 도입한다. API 키와 사용량 제한, 프롬프트 및 결과 저장은 백엔드에서 관리한다.

## 현재 제외 범위

- 공개 회원가입
- 소셜 로그인
- 실시간 공동 편집
- 파일 첨부
- 네이티브 모바일 앱
- AI 자동 분석 및 외부 AI API 연동
